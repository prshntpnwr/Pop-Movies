<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/movies_blue</item>
        <item name="colorPrimaryDark">@color/movies_dark_blue</item>
        <item name="colorAccent">@color/movies_light_blue</item>
    </style>

    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="AppTheme">
    </style>

</resources>